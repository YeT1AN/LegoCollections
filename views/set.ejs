<!DOCTYPE html>
<html lang="en" data-theme="valentine">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Lego Collection - <%= set.name %></title>
    <link rel="stylesheet" href="/css/main.css" />
    <script>
      document.addEventListener('DOMContentLoaded', async () => {
        let res = await fetch('https://quotable.io/random');
        let data = await res.json();
        document.getElementById(
          'quote'
        ).innerHTML = `"${data.content}"<br />- <em>${data.author}</em>`;
      });
    </script>
  </head>
  <body tabindex="-1">
    <%- include('partials/navbar', {page: '/lego/set'}) %>
    <div class="container mx-auto p-4">
      <div class="hero bg-base-200 rounded-lg p-6">
        <div class="hero-content text-center">
          <div class="max-w-md">
            <h1 class="text-5xl font-bold"><%= set.name %></h1>
            <p class="py-6">
              Below, you will find detailed information about the set: <%=
              set.name %>
            </p>
          </div>
        </div>
      </div>
      <br />
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
            <img
                class="w-full rounded-lg shadow-md"
                src="<%= set.img_url %>"
                alt="<%= set.name %> Set Image"
            />
        </div>
        <div>
            <h2 class="text-2xl font-bold mb-4"><%= set.name %></h2>
            <p class="mb-2"><span class="font-semibold">Year Released:</span> <%= set.year %></p>
            <p class="mb-2"><span class="font-semibold">Theme:</span> <%= set.Theme.name %></p>
            <p class="mb-4"><span class="font-semibold">Number of Parts:</span> <%= set.num_parts %></p>
            <p id="quote" class="italic mb-4"></p>
            <div class="flex gap-4">
              <a href="/lego/editSet/<%= set.set_num %>" class="bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded transition duration-300">Edit</a>
              <a href="#" onclick="history.back(); return false;" class="bg-pink-500 hover:bg-pink-600 text-white font-bold py-2 px-4 rounded transition duration-300">Return</a>
          </div>
          
        </div>
    </div>
    
    </div>
  </body>
</html>
</html>